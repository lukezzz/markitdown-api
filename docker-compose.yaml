services:
  markitdown-api:
    build:
      dockerfile: dockerfile
    hostname: markitdown-api
    ports:
      - "8490:8490"
    # command: uvicorn main:app --workers 1 --host=0.0.0.0 --port=80 --proxy-headers --reload
    # command: [ "fastapi", "dev", "--host=0.0.0.0", "--port=80" ]
    # command: ["fastapi", "run", "--host=0.0.0.0", "--port=80"]

