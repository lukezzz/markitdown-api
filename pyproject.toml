[project]
name = "markitdown-api"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastapi",
    "markitdown[all]",
    "markitdown-ocr",
    "openai",
    "python-dotenv",
    "python-multipart",
    "uvicorn",
]
